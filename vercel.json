{
  "buildCommand": "pnpm --filter @workspace/abq-store run build",
  "installCommand": "pnpm install --frozen-lockfile",
  "outputDirectory": "artifacts/abq-store/dist/public",
  "rewrites": [
    {
      "source": "/((?!api/).*)",
      "destination": "/index.html"
    }
  ]
}